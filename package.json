{
  "name": "khoaluan-backend",
  "version": "1.0.0",
  "description": "Server-side application",
  "main": "src/index.js",
  "scripts": {
    "start:up": "docker-compose --env-file .env.development up -d",
    "start:down": "docker-compose --env-file .env.development down",
    "start:prod": "NODE_ENV=production node .",
    "start:dev": "NODE_ENV=development nodemon src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "lth",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "mysql2": "^2.3.3",
    "nodemon": "^2.0.15",
    "sequelize": "^6.12.0-alpha.1"
  }
}
